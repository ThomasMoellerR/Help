ssh-keygen -t rsa

git remote -v		# Anzeigen von https oder git verbindung, es darf keine https verbindung drin sein, sondern eine git-verbindung

git remote rm origin

git remote add origin git@github.com:ThomasMoellerR/MQTT_Thermostat.git


git clone https://github.com/ThomasMoellerR/MQTT_Thermostat



git pull origin master





git clone git@github.com:ThomasMoellerR/MQTT_Thermostat.git


git branch # show branches

git branch newbranch # create new branch

git checkout newbranch # make it active

git log


// Developing

1. git branch develop    # create a new branch called "develop"  -> its like a snapshot
2. git branch            # shows all available branches
3. git checkout develop  # switches to branch "develop"
4. -make some changes in Readme.txt
5. git status            # shows a in red that the file Readme.txt was changed
6. git commit -am "added somethin new"    # create a commit
7. git log               # shows newest entry on the top -> type "q" for leaving
8. git checkout master   # go back to snapshot master











